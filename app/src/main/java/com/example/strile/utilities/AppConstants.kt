package com.example.strile.utilities

class AppConstants {
    companion object {
        const val TAG_LOG = "CUSTOM_LOG"

        const val NUMBER_OF_THREADS_FOR_DATABASE_WRITE_EXECUTOR = 8

        //bundle tags
        const val EMAIL = "email"
        const val RESULT = "result"
        const val NO_TEXT = "no_text"
        const val OK_TEXT = "ok_text"
    }
}